<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="site.metacoding.red.domain.teams.TeamsDao">

	<insert id="insert">
		INSERT INTO teams(teamName, stadiumId, location, createDate)
		VALUES(#{teamName}, #{stadiumId}, #{location}, now())
	</insert>

	<select id="findAll"
		resultType="site.metacoding.red.domain.teams.Teams">
		SELECT * FROM teams ORDER BY id DESC
	</select>

	<select id="findById"
		resultType="site.metacoding.red.domain.teams.Teams">
		SELECT * FROM teams WHERE id = #{id}
	</select>

	<insert id="update">
		UPDATE teams SET teamName = #{teamName},
		stardiumId = #{stardiumId},
		location = #{location}
		WHERE id = #{id}
	</insert>

	<insert id="deleteById">
		DELETE FROM teams WHERE id = #{id}
	</insert>
	
</mapper>