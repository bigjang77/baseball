<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.red.domain.player.PlayerDao">

	<insert id="insert">
		INSERT INTO player(playerName, teamId, position,
		createDate)
		VALUES(#{playerName}, #{teamId}, #{position}, now())
	</insert>

	<select id="findAll"
		resultType="site.metacoding.red.domain.player.Player">
		SELECT id, playerName, teamId, position, createDate
		FROM player
	</select>

	<select id="findById"
		resultType="site.metacoding.red.domain.player.Player">
		SELECT p.id, p.playerName "playerName", p.position, t.teamName "teamName", p.createDate
		FROM player p
		INNER JOIN team t
		ON p.teamId = t.id
	</select>

	<insert id="update">
		UPDATE player SET playerName = #{playerName},
		teamName = #{teamName},
		position = #{position}
		WHERE id = #{id}
	</insert>

	<delete id="deleteById">
		DELETE FROM player WHERE id = #{id}
	</delete>

</mapper>