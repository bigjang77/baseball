<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.red.domain.team.TeamDao">

	<select id="findAll" resultType="site.metacoding.red.web.dto.response.PositionDto">
	SELECT POSITION,
	Max(if(teamId=1, playerName, "")) "롯데",
	Max(if(teamId=2, playerName, "")) "키움",
	Max(if(teamId=3, playerName, "")) "기아"
	FROM player
	GROUP BY POSITION
	</select>

</mapper>