<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="site.metacoding.red.domain.stadiums.StadiumsDao">

	<insert id="insert">
		INSERT INTO stadiums(stadiumName, location, createDate)
		VALUES(#{stadiumName}, #{location}, now())
	</insert>

	<select id="findAll"
		resultType="site.metacoding.red.domain.stadiums.Stadiums">
		SELECT * FROM stadiums ORDER BY id DESC
	</select>

	<select id="findById"
		resultType="site.metacoding.red.domain.stadiums.Stadiums">
		SELECT * FROM stadiums WHERE id = #{id}
	</select>

	<insert id="update">
		UPDATE stadiums SET stadiumName = #{stadiumName},
		location =
		#{location}
		WHERE id = #{id}
	</insert>

	<insert id="deleteById">
		DELETE FROM stadiums WHERE id = #{id}
	</insert>
	
</mapper>